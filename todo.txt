Deleted code is in `git show 81ce1b2`. Rebuild in slices, per CONTRIBUTING.md.


rebuild (roughly this order):
- signals.ts: bulk-mail header tables; derive the IMAP fetch list from them so it can't drift
- headers.ts: RFC 5322 unfolding, repeated headers, DKIM d=, List-Id normalising
- messages + mailbox_sync tables; keep raw headers so the classifier can re-run offline
- upsert on those tables must not clobber body_text/group_key/category
- incremental sync: last-seen UID keyed by UIDVALIDITY, whose change invalidates the cache
- `UID FETCH n:*` always returns the last message, so skip uid <= sinceUid
- body download: BODYSTRUCTURE text part only, cap both fetch and stored size


next:
- classifier: score the signals, fill category + bulk_score
- grouping: group_key = List-Id > DKIM d= > From org domain; ESP shared domains over-merge
- bulk delete: gate hard, give it its own read-write helper, never loosen withMailbox
- manual block-list alongside the header signals
- sync deletions: server-side deletes still linger in the local cache
- move full-mailbox sync off the request path
- multi-account dashboard polish


known issues:
- no error.tsx or loading.tsx anywhere — dropped for v0. Failures fall through to
  Next's default page and navigation hangs silently during the IMAP round trip
- every message click opens a fresh IMAP connection (~1s); fix via the cache, not client state
- viewer is text-only; real HTML needs sanitising + sandboxed iframe, remote images off
- postcss.config.mjs fails silently: build passes, Tailwind emits an empty stylesheet
- crypto.ts has no key rotation — changing the key strands every stored password
- no test mailbox: hack/seed-testmail.sh (Dovecot/GreenMail) + a synthetic generator
