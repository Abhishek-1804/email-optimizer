# Dependencies
/node_modules

# Self-contained toolchain downloaded by hack/install-deps.sh
/bin

# Build output
/.next
/out
/build
next-env.d.ts
*.tsbuildinfo

# Local SQLite database. Holds encrypted IMAP app passwords and cached mail —
# never commit it. `just clean` removes it.
/data

# Secrets
.env*
!.env.example

# Logs
*.log
npm-debug.log*

# Test coverage
/coverage

# Deployment
/.vercel

# Editors and OS junk. Deliberately unanchored — these turn up at any depth.
.idea/
.vscode/
.DS_Store
*.pem
*.swp
